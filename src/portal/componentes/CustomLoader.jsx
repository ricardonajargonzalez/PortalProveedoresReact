


import { CircularProgress, Typography } from '@mui/material';

const CustomLoader  = ({ data }) => (
    <div>
        <center><CircularProgress /></center>
        <center>Cargando . . .</center>
    </div>
   

  );
  
  export default CustomLoader ;